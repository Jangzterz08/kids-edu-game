---
phase: 02-polish-ux
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - client/src/lib/avatars.js
  - client/src/pages/KidHome.jsx
  - client/src/pages/ParentDashboard.jsx
  - client/src/pages/Login.jsx
  - client/src/pages/ClassroomDetail.jsx
  - client/src/components/layout/KidLayout.jsx
  - client/src/components/classroom/LeaderboardTable.jsx
  - client/src/components/kid/KidCard.jsx
  - client/src/pages/MiniGame.jsx
  - client/src/hooks/useProgress.js
  - client/src/data/index.js
  - client/src/pages/ModuleComplete.jsx
  - server/src/routes/dailyChallenge.js
autonomous: true
requirements: [POL-07, POL-08, POL-09]

must_haves:
  truths:
    - "A parent whose kid bought avatar 9-16 in the store sees the correct emoji in ParentDashboard (no fallback bear)"
    - "The client does not contain a computeStars function — stars come from the server"
    - "Visiting /api/daily-challenge/today returns a JSON object with moduleSlug"
    - "The client does not contain getDailyChallengeSlug — the slug comes from the server"
  artifacts:
    - path: "client/src/lib/avatars.js"
      provides: "Unified AVATAR_EMOJIS constant with 16 entries"
      exports: ["AVATAR_EMOJIS"]
      min_lines: 5
    - path: "server/src/routes/dailyChallenge.js"
      provides: "GET /today endpoint returning daily moduleSlug"
      contains: "router.get('/today'"
  key_links:
    - from: "client/src/pages/ParentDashboard.jsx"
      to: "client/src/lib/avatars.js"
      via: "named import"
      pattern: "import.*AVATAR_EMOJIS.*from.*lib/avatars"
    - from: "client/src/pages/ModuleComplete.jsx"
      to: "/api/daily-challenge/today"
      via: "fetch call"
      pattern: "daily-challenge/today"
---

<objective>
Unify the avatar emoji map into a single shared constant, remove client-side star computation in favor of server response, and add a /api/daily-challenge/today endpoint to replace client-side slug computation.

Purpose: ParentDashboard shows a fallback bear for half the store avatars because it only has 8 entries. The client duplicates server logic for stars and daily challenge slugs, creating a maintenance risk. This plan eliminates all three issues.
Output: avatars.js shared constant, 7 files updated to import it, computeStars removed from MiniGame.jsx, /api/daily-challenge/today endpoint, getDailyChallengeSlug removed from client.
</objective>

<execution_context>
@/Users/Ja_Jang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Ja_Jang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-polish-ux/02-CONTEXT.md
@.planning/phases/02-polish-ux/02-RESEARCH.md
@.planning/phases/02-polish-ux/02-01-SUMMARY.md

<interfaces>
<!-- Avatar files to update. Research confirmed 7 files with inline AVATAR_EMOJIS definitions. -->
<!-- The canonical 16-avatar set is in KidHome.jsx. ParentDashboard has only 8. -->

Files containing inline AVATAR_EMOJIS (all must import from lib/avatars.js):
1. client/src/pages/KidHome.jsx
2. client/src/pages/ParentDashboard.jsx (only 8 avatars — THIS IS THE BUG)
3. client/src/pages/Login.jsx
4. client/src/pages/ClassroomDetail.jsx
5. client/src/components/layout/KidLayout.jsx
6. client/src/components/classroom/LeaderboardTable.jsx
7. client/src/components/kid/KidCard.jsx

From server/src/routes/dailyChallenge.js (has getChallengeSlug function and /:kidId route):
- getChallengeSlug() uses DAILY_MODULE_SLUGS[dayOfYear % DAILY_MODULE_SLUGS.length]
- Route /:kidId exists — /today MUST be registered BEFORE /:kidId to avoid shadowing

From client/src/data/index.js (exports getDailyChallengeSlug — must be removed):
- getDailyChallengeSlug() duplicates the same day-of-year formula
- Called from client/src/pages/ModuleComplete.jsx

From client/src/pages/MiniGame.jsx:
- Contains computeStars() function
- handleGameComplete calls computeStars(newScores) and passes result to recordLesson
- After Plan 01, useProgress.js captures API response — MiniGame should stop computing stars
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared avatar constant, update all 7 consumer files</name>
  <files>client/src/lib/avatars.js, client/src/pages/KidHome.jsx, client/src/pages/ParentDashboard.jsx, client/src/pages/Login.jsx, client/src/pages/ClassroomDetail.jsx, client/src/components/layout/KidLayout.jsx, client/src/components/classroom/LeaderboardTable.jsx, client/src/components/kid/KidCard.jsx</files>
  <read_first>
    - client/src/pages/KidHome.jsx (canonical 16-avatar set — copy this exact set)
    - client/src/pages/ParentDashboard.jsx (buggy 8-avatar set — will be replaced)
    - client/src/pages/Login.jsx (find AVATAR_EMOJIS definition)
    - client/src/pages/ClassroomDetail.jsx (find AVATAR_EMOJIS definition)
    - client/src/components/layout/KidLayout.jsx (find AVATAR_EMOJIS definition)
    - client/src/components/classroom/LeaderboardTable.jsx (find AVATAR_EMOJIS definition)
    - client/src/components/kid/KidCard.jsx (find AVATAR_EMOJIS definition)
    - .planning/phases/02-polish-ux/02-CONTEXT.md (locked decision — 16 entries, plain object)
  </read_first>
  <action>
**Step 1: Create `client/src/lib/avatars.js`**

```js
export const AVATAR_EMOJIS = {
  bear: '🐻',
  lion: '🦁',
  rabbit: '🐰',
  cat: '🐱',
  dog: '🐶',
  owl: '🦉',
  fox: '🦊',
  penguin: '🐧',
  frog: '🐸',
  chick: '🐥',
  hamster: '🐹',
  panda: '🐼',
  butterfly: '🦋',
  dragon: '🐉',
  dino: '🦖',
  unicorn: '🦄',
};
```

**Step 2: Update all 7 consumer files**

For each file listed below, the executor must:
1. Add `import { AVATAR_EMOJIS } from '../lib/avatars';` (adjust relative path based on file depth — pages use `'../lib/avatars'`, components use `'../../lib/avatars'`)
2. Delete the inline `AVATAR_EMOJIS` or equivalent object definition (search for `const AVATAR_EMOJIS` or any object literal containing `bear:` and `cat:` as emoji values)
3. Ensure the rest of the file still references `AVATAR_EMOJIS` by the same name (no rename needed)

Exact import paths per file:
| File | Import Path |
|------|------------|
| `client/src/pages/KidHome.jsx` | `'../lib/avatars'` |
| `client/src/pages/ParentDashboard.jsx` | `'../lib/avatars'` |
| `client/src/pages/Login.jsx` | `'../lib/avatars'` |
| `client/src/pages/ClassroomDetail.jsx` | `'../lib/avatars'` |
| `client/src/components/layout/KidLayout.jsx` | `'../../lib/avatars'` |
| `client/src/components/classroom/LeaderboardTable.jsx` | `'../../lib/avatars'` |
| `client/src/components/kid/KidCard.jsx` | `'../../lib/avatars'` |

**Step 3: Verify no other files have inline avatar maps**

Run `grep -rn "AVATAR_EMOJIS\|bear.*🐻.*cat.*🐱\|🐻.*🐱" client/src/ --include="*.jsx" --include="*.js"` after updates. The only match should be `client/src/lib/avatars.js` (the definition) and the import lines in the 7 consumer files. No inline definitions should remain.
  </action>
  <verify>
    <automated>cd /Users/Ja_Jang/Application/kids-edu-game && cat client/src/lib/avatars.js | grep -c ":" | xargs -I{} test {} -eq 16 && echo "16 entries OK" && for f in client/src/pages/KidHome.jsx client/src/pages/ParentDashboard.jsx client/src/pages/Login.jsx client/src/pages/ClassroomDetail.jsx client/src/components/layout/KidLayout.jsx client/src/components/classroom/LeaderboardTable.jsx client/src/components/kid/KidCard.jsx; do grep -q "import.*AVATAR_EMOJIS.*from.*avatars" "$f" || echo "MISSING import in $f"; done && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - client/src/lib/avatars.js exists and exports `AVATAR_EMOJIS` with exactly 16 entries (bear through unicorn)
    - client/src/lib/avatars.js contains `export const AVATAR_EMOJIS`
    - client/src/pages/KidHome.jsx contains `import { AVATAR_EMOJIS } from '../lib/avatars'`
    - client/src/pages/KidHome.jsx does NOT contain an inline `const AVATAR_EMOJIS =` definition
    - client/src/pages/ParentDashboard.jsx contains `import { AVATAR_EMOJIS } from '../lib/avatars'`
    - client/src/pages/ParentDashboard.jsx does NOT contain an inline avatar definition with only 8 entries
    - client/src/pages/Login.jsx contains `import.*AVATAR_EMOJIS.*from.*avatars`
    - client/src/pages/ClassroomDetail.jsx contains `import.*AVATAR_EMOJIS.*from.*avatars`
    - client/src/components/layout/KidLayout.jsx contains `import.*AVATAR_EMOJIS.*from.*avatars`
    - client/src/components/classroom/LeaderboardTable.jsx contains `import.*AVATAR_EMOJIS.*from.*avatars`
    - client/src/components/kid/KidCard.jsx contains `import.*AVATAR_EMOJIS.*from.*avatars`
    - No file in client/src/ other than lib/avatars.js contains `const AVATAR_EMOJIS =` (inline definitions removed)
  </acceptance_criteria>
  <done>Single AVATAR_EMOJIS constant with 16 entries in lib/avatars.js, all 7 consumer files import from it, no inline definitions remain, ParentDashboard now shows correct emoji for all 16 avatars</done>
</task>

<task type="auto">
  <name>Task 2: Remove client computeStars, add /api/daily-challenge/today, remove client getDailyChallengeSlug</name>
  <files>client/src/pages/MiniGame.jsx, client/src/data/index.js, client/src/pages/ModuleComplete.jsx, server/src/routes/dailyChallenge.js</files>
  <read_first>
    - client/src/pages/MiniGame.jsx (find computeStars function and all calls to it — must delete function and update handleGameComplete)
    - client/src/hooks/useProgress.js (after Plan 01, recordLesson returns server response — check if starsEarned is available)
    - server/src/services/progressSync.js (verify server computes starsEarned — check what fields are returned)
    - server/src/routes/progress.js (verify /lesson/:lessonId response includes starsEarned)
    - server/src/routes/dailyChallenge.js (find getChallengeSlug function and existing /:kidId route — /today must go BEFORE /:kidId)
    - client/src/data/index.js (find getDailyChallengeSlug export — must be deleted)
    - client/src/pages/ModuleComplete.jsx (find getDailyChallengeSlug call sites — must be replaced with API fetch)
    - .planning/phases/02-polish-ux/02-CONTEXT.md (locked decisions for POL-08 and POL-09)
  </read_first>
  <action>
**Part A: Remove computeStars from client (POL-08)**

1. Read `client/src/pages/MiniGame.jsx` fully. Find the `computeStars` function definition and all call sites.
2. Delete the `computeStars` function entirely.
3. In `handleGameComplete` (or equivalent), find where `computeStars(newScores)` is called and its return value is assigned (e.g., `starsEarned: computeStars(newScores)`).
4. Remove the `starsEarned` field from the `update` object passed to `recordLesson`. The server already computes `starsEarned` in `progressSync.js` and includes it in the response.
5. If the component displays stars after game completion (e.g., passing starsEarned to a results screen), it should read the value from the server response. After Plan 01, `recordLesson` in useProgress.js returns the API response. If MiniGame.jsx needs to display stars:
   - Change `recordLesson(lessonId, update)` to `const result = await recordLesson(lessonId, update)` (if not already)
   - Use `result.starsEarned` (or check the actual field name in the response) for display
6. Verify the server response shape: read `server/src/routes/progress.js` POST handler for `/lesson/:lessonId`. Check that `starsEarned` is in `res.json(...)`. If it is NOT, the executor must add it by reading the return value from `upsertProgress` (which computes stars internally).

**Part B: Add /api/daily-challenge/today endpoint (POL-09)**

1. Read `server/src/routes/dailyChallenge.js` fully.
2. Find the `getChallengeSlug` function (or equivalent — it uses `DAILY_MODULE_SLUGS[dayOfYear % DAILY_MODULE_SLUGS.length]`).
3. Add a new route BEFORE the existing `/:kidId` route (Express route order matters — `/today` must not be shadowed by `/:kidId`):

```js
// Public — no auth needed, returns today's challenge module slug
router.get('/today', (req, res) => {
  res.json({ moduleSlug: getChallengeSlug() });
});
```

If `getChallengeSlug` is defined inline in the `/:kidId` handler rather than as a standalone function, extract it first.

4. The route requires NO authentication — it just returns the daily slug. But check how the router is mounted in `server/src/index.js`. If the daily-challenge router is mounted behind `requireAuth`, the `/today` sub-route will also require auth. If so, either:
   - Mount a separate mini-router for `/api/daily-challenge/today` BEFORE the auth-gated mount, OR
   - Skip auth in the handler itself (check if there's a pattern for this)

**Part C: Remove getDailyChallengeSlug from client (POL-09)**

1. Read `client/src/data/index.js`. Find and delete the `getDailyChallengeSlug` function and its export.
2. Also delete any supporting constants used ONLY by that function (e.g., a `DAILY_MODULE_SLUGS` array if it exists only for this purpose — check if other functions use it first).
3. Read `client/src/pages/ModuleComplete.jsx`. Find all call sites of `getDailyChallengeSlug()`.
4. Replace each call with a fetch to the new endpoint:
   - `const res = await fetch('/api/daily-challenge/today'); const { moduleSlug } = await res.json();`
   - Or use the existing `api.get('/api/daily-challenge/today')` helper from `lib/api.js` if it works for unauthenticated requests.
5. Since the fetch is async and the previous function was synchronous, the calling code may need to be restructured. If `getDailyChallengeSlug()` was called in a `useMemo` or render-time expression, it must move to a `useEffect` + `useState` pattern.
6. Grep for any other callers: `grep -rn "getDailyChallengeSlug" client/src/` — update ALL call sites, not just ModuleComplete.jsx.
  </action>
  <verify>
    <automated>cd /Users/Ja_Jang/Application/kids-edu-game && grep -c "computeStars" client/src/pages/MiniGame.jsx | xargs -I{} test {} -eq 0 && echo "computeStars removed" && grep -c "getDailyChallengeSlug" client/src/data/index.js | xargs -I{} test {} -eq 0 && echo "getDailyChallengeSlug removed" && grep "router.get.*today" server/src/routes/dailyChallenge.js && echo "today endpoint added" && grep "daily-challenge/today" client/src/pages/ModuleComplete.jsx && echo "client calls endpoint" && cd server && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - client/src/pages/MiniGame.jsx does NOT contain `computeStars` (function deleted, no calls remain)
    - client/src/pages/MiniGame.jsx does NOT contain `starsEarned: computeStars` (removed from update object)
    - client/src/data/index.js does NOT contain `getDailyChallengeSlug` (function and export deleted)
    - client/src/pages/ModuleComplete.jsx does NOT contain `getDailyChallengeSlug` (replaced with API call)
    - client/src/pages/ModuleComplete.jsx contains `daily-challenge/today` (fetches from server)
    - server/src/routes/dailyChallenge.js contains `router.get('/today'` BEFORE `router.get('/:kidId'`
    - server/src/routes/dailyChallenge.js /today handler returns `res.json` with `moduleSlug`
    - `grep -rn "computeStars" client/src/` returns no matches
    - `grep -rn "getDailyChallengeSlug" client/src/` returns no matches
    - Server tests pass: `cd server && npx vitest run` exits 0
  </acceptance_criteria>
  <done>computeStars deleted from client (server computes stars), /api/daily-challenge/today returns daily slug, getDailyChallengeSlug deleted from client data/index.js and all callers use the API</done>
</task>

</tasks>

<verification>
1. `grep -rn "computeStars" client/src/` — returns empty (POL-08 complete)
2. `grep -rn "getDailyChallengeSlug" client/src/` — returns empty (POL-09 client complete)
3. `grep "router.get.*today" server/src/routes/dailyChallenge.js` — /today route exists
4. `grep -c "AVATAR_EMOJIS" client/src/lib/avatars.js` — constant exists
5. `cd /Users/Ja_Jang/Application/kids-edu-game/client && npm run build` — build succeeds
6. `cd /Users/Ja_Jang/Application/kids-edu-game/server && npx vitest run --reporter=verbose` — tests pass
</verification>

<success_criteria>
- AVATAR_EMOJIS is a single shared constant in lib/avatars.js with 16 entries
- All 7 consumer files import from lib/avatars.js, no inline definitions remain
- ParentDashboard will show correct emoji for all 16 avatars (no fallback bear)
- computeStars function deleted from MiniGame.jsx, client trusts server starsEarned
- /api/daily-challenge/today endpoint exists and returns { moduleSlug } without auth
- /today is registered before /:kidId in dailyChallenge.js (no route shadowing)
- getDailyChallengeSlug deleted from client, all callers use API endpoint
- Client builds without errors, server tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-polish-ux/02-03-SUMMARY.md`
</output>
